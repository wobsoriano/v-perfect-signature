{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "deploy": "turbo run deploy",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^1.1.4"
  },
  "packageManager": "yarn@1.22.10"
}
