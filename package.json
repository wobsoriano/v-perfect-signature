{
    "name": "monorepo",
    "private": true,
    "workspaces": [
      "packages/*"
    ],
    "scripts": {
      "dev": "yarn workspace example dev",
      "lib:watch": "yarn workspace v-signature-pad run build:watch",
      "lib": "yarn workspace v-signature-pad",
      "example": "yarn workspace example"
    }
}